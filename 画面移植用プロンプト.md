# 防災アプリ統合実装用プロンプト

あなたはソフトウェア実装担当AIです。以下の要件に従い、既存システムへホーム画面、避難所登録、避難所詳細、指示発信ボードの機能を統合実装してください。

## 目的

既存のホーム画面、避難所検索画面、避難所登録画面などの構成、認証方式、DB、データ保存方式、UI規約を最初に確認してください。質問や実装案の提示だけで止まらず、必要なファイルを編集し、動作確認まで完了してください。既存の無関係な機能やデータは壊さないでください。

次の機能を追加・統合する。

1. 防災アプリホーム画面
2. 避難所登録・編集・避難者数更新
3. 避難所詳細画面
4. 指示発信ボード画面

既存のルーティング、認証、データ保存方式、CSS設計を優先して利用すること。既存機能を削除したり、無関係な画面を大幅に変更したりしないこと。

---

## 1. 避難所詳細画面

### 画面遷移

- 避難所検索画面に登録済みの全施設を表示する。
- 各施設名を押すと、その施設の詳細画面へ移動できる。
- URL例: `/shelter/<shelter_id>`
- 存在しない避難所IDの場合は404または適切なエラー表示にする。
- 詳細画面の上部と下部に「避難所検索に戻る」リンクを配置する。
- リンクはハードコードせず、対象フレームワークのURL生成機能を利用する。

### 表示内容

避難所データから次の基本情報を表示する。

- 避難所名
- 住所
- Google Mapリンク
- 収容人数
- 開設状況
- 利用可能時間
- 連絡先

値が存在しない場合は「未登録」と表示する。ただし連絡先は `contact` を優先し、なければ `phone` を使用する。

Google Mapリンクがデータにない場合は、住所または避難所名を検索語にしたGoogle Maps検索URLを生成する。

Google Mapを別タブで開く場合は、次の属性を付ける。

- `target="_blank"`
- `rel="noopener noreferrer"`

### 設備情報

次の設備情報をチェックボックス形式で表示する。

- トイレ
- Wi-Fi
- 空調
- 発電機

チェックボックスは詳細表示専用とし、ユーザーが変更できないよう disabled にする。チェック済みを「あり」、未チェックを「なし」として視覚的に区別する。

### バリアフリー情報

次の対応状況をチェックボックス形式で表示する。

- 車いす対応
- スロープ
- 多目的トイレ

こちらも詳細表示専用とし、ユーザーが変更できないよう disabled にする。

### 表示・レスポンシブ要件

- 避難所名をページタイトルまたは見出しに含める。
- 基本情報はラベルと値を比較しやすい表形式で表示する。
- 長い住所やURLが画面からはみ出さないようにする。
- スマートフォンでも表が読めるようにする。
- 既存サイトの色、余白、フォント、ボタン、リンクのデザインに合わせる。
- 既存の共通レイアウトを継承し、独自のページ全体レイアウトを重複実装しない。

---

## 2. 指示発信ボード画面

### 認証

- 指示発信ボードはログイン必須にする。
- 未ログインで `/board` にアクセスした場合はログイン画面へ移動する。
- ログイン成功後は、ログイン前にアクセスしようとした `/board` に戻す。
- 避難所登録画面へ誤ってリダイレクトしないようにする。
- リダイレクト先は同一アプリ内の相対パスで保持する。
- 外部URLへリダイレクトできないようにする。

### レイアウト

画面を左メニューと右側のメイン領域に分ける。

#### 左側メニュー

上から次の順で表示する。

1. 検索
2. 情報発信
3. 発信情報の閲覧

各メニューを押すと、同一ページ内の対応するセクションへ移動できるようにする。アンカーリンク、タブ、または既存システムに合うナビゲーション方式を利用する。

#### 中央・メイン領域

上から次の順で配置する。

1. 検索ワード入力欄と検索ボタン
2. 情報入力欄
3. 発信情報の閲覧

#### 送信ボタン

情報入力欄の右下に送信ボタンを配置する。

---

## 3. 指示発信ボードの機能

### 検索

- 検索ワードを入力できる。
- 発信内容と避難所名を対象に検索する。
- 検索結果がない場合は「該当する発信情報はありません」などのメッセージを表示する。
- 検索後も検索ワードを入力欄に保持する。
- 検索はGETパラメータなど、既存システムに適した方式で実装する。

### 情報発信

次の入力項目を用意する。

- 避難所名: 任意入力
- 情報: 必須入力

送信時の要件:

- 情報が空の場合は保存しない。
- サーバー側でも必須チェックを行う。
- 発信対象は住民とする。
- 新しい発信情報を一覧の先頭に表示する。
- 作成日時と更新日時を保存する。
- 初期ステータスは「発信中」とする。
- 既存システムのデータ保存機能を使用する。
- 保存後は二重送信を防ぐため、GETの一覧画面へリダイレクトする。
- 入力値はHTMLエスケープし、XSSを防止する。

### 本文中の強調表示

対象者をチェックボックスや選択欄で指定するのではなく、発信本文に `@(文字)` と入力して指定する。

例:

```text
@(高齢者)の方は早めに避難してください。
@(妊婦)の方は優先避難所へ移動してください。
```

表示時の要件:

- `@(...)` の括弧内の文字だけを強調表示する。
- `@` と括弧は表示から除外するか、既存デザインに合わせて控えめに表示する。
- 情報入力中にも、入力欄の下などに発信時の表示プレビューを表示する。
- プレビューは入力イベントごとに更新し、`@(文字)` の強調結果をリアルタイムで確認できるようにする。
- `@(高齢者)`、`@(妊婦)`、`@(女性)` など、任意の文字を強調対象にできるようにする。
- 括弧内にHTMLやスクリプトが入力されても実行されないよう、入力値をエスケープしてから強調処理する。
- 不正な括弧や空の `@()` は通常の文字列として安全に表示する。

データ例:

```json
{
  "id": 5,
  "target": "住民",
  "content": "大雨のため河川付近には近づかないでください",
  "shelter": "",
  "status": "発信中",
  "created_at": "2026年08月20日 12:00",
  "updated_at": "2026年08月20日 12:00"
}
```

### 発信情報の閲覧

- 住民向けの発信情報だけを表示する。
- 各発信情報について、内容、関連する避難所名、作成日時、ステータスを表示する。
- 情報がない場合は空状態メッセージを表示する。
- 一覧表示は既存データの順序や仕様を尊重する。

---

## 4. 既存画面との統合

- 共通ヘッダーやナビゲーションに「避難所検索」「指示発信ボード」への導線を追加する。
- 既存のホーム画面のレイアウトを壊さない。
- 既存の避難所登録画面の処理を変更しない。
- 避難所検索画面では登録済み避難所を一覧表示し、各施設から詳細画面へ移動できるようにする。
- 既存のログイン処理、セッション管理、権限管理を再利用する。
- URL、テンプレート、コンポーネント、APIの命名は移植先の規約に合わせる。
- CSSは移植先のデザインシステムに合わせ、既存スタイルを上書きしすぎない。
- PCとスマートフォンの両方で、文字やボタンが重ならないようにする。

---

## 5. セキュリティと品質

- ログイン必須の判定はサーバー側で行う。
- リダイレクト先URLを検証し、外部サイトへ転送しない。
- POST送信にはCSRF対策を追加する。移植先のフレームワークに標準機能があれば利用する。
- 入力値を適切に検証・エスケープする。
- ファイル保存を使う場合は同時書き込みによる破損に注意する。可能なら既存DBやリポジトリの保存層を使用する。
- 既存のテストや機能を壊さない。
- 移植先の既存テスト実行方法に従って検証する。

---

## 6. 受け入れ条件

次の条件をすべて満たすこと。

- 避難所検索画面に登録済み避難所が表示される。
- 避難所名から正しい避難所詳細画面へ移動できる。
- 避難所詳細画面の上部と下部から避難所検索へ戻れる。
- 詳細画面に基本情報、設備情報、バリアフリー情報が表示される。
- 未ログイン状態で指示発信ボードへアクセスするとログイン画面が表示される。
- 指示発信ボードへアクセスしてからログインすると、避難所登録画面ではなく指示発信ボードへ戻る。
- 左メニューに「検索」「情報発信」「発信情報の閲覧」が順番どおり表示される。
- 検索ワードで発信内容または避難所名を絞り込める。
- 情報を送信すると発信情報一覧の先頭に追加される。
- 空の情報は送信できない。
- `@(高齢者)` などの記法で括弧内の文字が強調表示される。
- 情報入力中のプレビューで、発信時の強調表示を確認できる。
- 発信情報の閲覧で住民向け情報だけを確認できる。
- PCとスマートフォンの両方でレイアウトが崩れない。
- 既存のホーム画面、避難所登録画面、ログイン画面が従来どおり動作する。

実装後、変更したファイル、利用したルート、データ保存方法、実行したテストを簡潔に報告してください。

---

## 7. 避難所登録機能

### 管理者ログイン

- 避難所登録、新規登録の保存、既存避難所の編集、避難者数更新は管理者だけが利用できるようにする。
- 未ログイン状態で保護画面へアクセスした場合はログイン画面へ遷移する。
- ログイン前のURLを安全に保持し、ログイン成功後は元のURLへ戻す。不正または外部URLの場合は避難所登録画面へ戻す。
- ログイン失敗時は「パスワードが正しくありません。」を表示する。
- 既存の認証基盤を優先し、パスワードをソースコードへ平文でハードコードしない。認証基盤がない場合は、安全なハッシュとセッションを利用する。
- CSRF対策、セッションCookieの安全属性、ログイン試行制限など、対象システムの標準的な対策を適用する。
- ログアウト時はセッションを破棄する。

### 登録フォーム

管理者向けの「避難所登録」ページを作成し、送信ボタンの表示は「入力内容を確認する」とする。画面上部に「登録済み避難所一覧」「避難者数更新」への導線を置く。

#### 基本情報

| 表示名 | フィールド名 | UI | 必須・制約 |
|---|---|---|---|
| 避難所名 | `name` | 1行テキスト | 必須、空白のみ禁止 |
| 住所 | `address` | 1行テキスト | 必須、空白のみ禁止 |
| 避難所種別 | `shelter_type` | セレクト | 必須、指定避難所・福祉避難所・緊急避難場所・その他 |
| 最大収容人数 | `capacity` | 数値 | 必須、0以上の整数 |
| 現在の収容人数 | `current_capacity` | 数値 | 必須、0以上、最大収容人数以下 |

#### 設備情報

フィールド名 `facilities` の複数選択チェックボックスを用意する。未選択は許可し、選択値をすべて保存する。

- トイレ
- Wi-Fi
- 発電機
- 空調
- 充電設備
- 給水設備
- 医療・救護設備

#### バリアフリー・要配慮者対応

フィールド名 `barrier_free` の複数選択チェックボックスを用意する。

- 車椅子対応
- スロープ
- 多目的トイレ
- エレベーター
- 妊婦・乳幼児対応
- 授乳室
- おむつ交換スペース

#### 駐車場

フィールド名 `parking` の複数選択チェックボックスを用意し、「□ 駐車場あり」のように表示する。

- 駐車場あり
- 駐車場なし
- 大型車対応

#### その他・開設・連絡先

| 表示名 | フィールド名 | UI | 必須・選択肢 |
|---|---|---|---|
| ペット | `pets_allowed` | セレクト | 必須、可・不可・専用スペースあり |
| 備考 | `notes` | 複数行テキスト | 任意 |
| 開設状況 | `status` | セレクト | 必須、開設・閉鎖・準備中 |
| 利用可能時間 | `hours` | 1行テキスト | 必須、空白のみ禁止 |
| 電話番号 | `contact_phone` | 電話番号 | 必須、対象システムの形式 |
| 担当部署 | `contact_department` | 1行テキスト | 必須、空白のみ禁止 |

すべての入力項目に適切なラベル、`name`、アクセシブルな関連付け、具体的な日本語エラーを付ける。PCとスマートフォンの両方で利用できるレスポンシブ画面にする。

### サーバー側バリデーション

- 必須項目の未入力・空白のみを拒否する。
- `capacity` と `current_capacity` は整数、0以上、`current_capacity <= capacity` を検証する。
- セレクト項目は定義済みの選択肢だけを許可する。
- 複数選択項目は定義済みの選択肢だけを許可し、重複と不正値を除外する。
- 電話番号、文字数、入力サイズを検証する。
- エラー時は入力値と選択済みチェックボックスを保持してフォームを再表示する。
- hidden inputを使う確認画面でも、値のエスケープとサーバー側再検証を行う。

### 入力確認・保存・完了

- フォーム送信後はいきなり保存せず、「入力内容の確認」画面を表示する。
- 確認画面には全項目を表示し、複数選択が未入力なら「未入力」、空の備考なら「なし」と表示する。
- 「登録」と「入力内容を修正する」を用意する。
- 修正へ戻るときも全入力値とチェック状態を保持する。
- 登録成功後は「登録完了しました！」と「避難所情報の登録が完了しました。」を表示する。
- 「避難所登録に戻る」リンクを用意する。
- 既存DBまたは既存の永続化方式を使う。JSONの場合は既存形式を壊さずUTF-8で保存する。
- `id` は一意にし、新規登録では安全な採番方式を使う。
- 保存失敗時はメモリ上だけ変更された状態を残さず、エラーを表示する。
- 二重送信や同時更新でデータが壊れないようにする。
- 既存データに `parking`、`pets_allowed`、`current_capacity` がなくても安全な初期値で表示する。

避難所データの論理構造は次の形式を基本とする。

```json
{
  "id": 1,
  "name": "避難所名",
  "address": "住所",
  "capacity": 120,
  "current_capacity": 20,
  "shelter_type": "福祉避難所",
  "pets_allowed": "可",
  "facilities": ["トイレ", "空調"],
  "barrier_free": ["スロープ"],
  "parking": ["駐車場あり", "大型車対応"],
  "status": "開設",
  "hours": "24時間",
  "contact_phone": "000-0000-0000",
  "contact_department": "担当部署",
  "notes": "備考"
}
```

## 8. 登録済み避難所一覧と編集

- 登録済み避難所を一覧表示する。
- 各行に避難所名、住所、種別、現在の収容人数/最大収容人数、開設状況を表示する。
- 避難所名から編集フォームへ移動する。
- 0件の場合は「該当する避難所が見つかりませんでした」等の空状態を表示する。
- 「検索画面に戻る」「トップページへ戻る」導線を用意する。
- 編集フォームは登録フォームと同じ項目を使用し、既存値と複数選択状態を復元する。
- 編集中のIDを保持し、保存時は新しいIDを発行せず対象レコードだけを更新する。
- 不正または存在しないIDは404等の標準エラーにする。

## 9. 避難者数更新

管理者向けの「避難者数更新」画面を作成する。

- 登録済み避難所をセレクトボックスで選択する。
- 選択した避難所の最大収容人数と現在の避難者数を表示する。
- 現在の避難者数を数値入力できる。
- `-1`、`+1`、`-5`、`+5` の調整ボタンを用意する。
- 調整値は0未満にならず、最大収容人数を超えないようにする。
- 現在の避難者数、空き人数、混雑率、状況を即時更新する。
- 混雑率は `現在の避難者数 / 最大収容人数 * 100` とし、最大収容人数が0なら0%とする。
- 状況は、満員を「🔴 満員」、最大の70%以上を「🟡 やや混雑」、それ以外を「🟢 余裕あり」とする。
- 「更新する」で保存し、成功時は「避難者数を更新しました。」を表示する。
- サーバー側でも0以上かつ最大収容人数以下を検証する。
- 対象未選択は「避難所を選択してください。」、保存失敗は「避難者数を更新できませんでした。」と表示する。
- 0件の場合は「登録済みの避難所がありません。」と表示する。
- 画面下部のリンクは「登録フォームに戻る」とする。

## 10. JSON API

対象システムにAPI機能がある場合、`GET /shelters` 相当のエンドポイントを用意する。

- 任意の地区パラメータを受け取り、該当する避難所だけを返す。
- 地区指定がなければ全件を返す。
- 結果があれば避難所の全データをJSONで返す。
- 結果がなければHTTP 404とエラーJSONを返す。
- `parking` を含む保存済みの全項目を返す。

## 11. 統合後の画面遷移

```text
未ログイン -> ログイン -> 元の保護画面
避難所登録 -> 入力内容の確認 -> 登録完了 -> 避難所登録
入力内容の確認 -> 登録完了
入力内容の確認 -> 入力値を保持した避難所登録
登録済み避難所一覧 -> 避難所編集
登録済み避難所一覧 -> 避難所検索
登録済み避難所一覧 -> トップページ
避難者数更新 -> 登録フォームに戻る
避難所検索 -> 避難所詳細
避難所詳細 -> 避難所検索
指示発信ボード -> 検索・情報発信・発信情報の閲覧
```

## 12. 統合後の受け入れテスト

次の操作を実際にテストする。

1. 未ログインで登録画面と指示発信ボードへアクセスし、ログイン後に目的の画面へ戻る。
2. 必須項目と駐車場項目を入力し、確認画面で全項目が表示される。
3. 確認画面から修正画面へ戻ったとき、全入力値とチェック状態が保持される。
4. 登録後、一覧と避難所検索に新しい避難所が表示される。
5. 一覧から編集し、駐車場を含む既存値が復元され、更新できる。
6. 避難者数更新で人数、空き、混雑率、状況が正しく変化し、保存後も維持される。
7. 不正な人数、上限超過、不正ID、不正選択肢、保存失敗時に適切なエラーになる。
8. 避難所名から詳細画面へ移動し、基本情報、設備、バリアフリー情報を確認できる。
9. 詳細画面の上部と下部から避難所検索へ戻れる。
10. `@(高齢者)` などの記法が発信者の入力中プレビューと閲覧画面の両方で強調される。
11. 指示発信ボードの検索、送信、住民向け発信情報の閲覧が動作する。
12. スマートフォン幅でも入力項目、タグ、ボタン、表が画面からはみ出さない。
13. 既存のホーム画面、ログイン画面、避難所登録画面など無関係な機能が従来どおり動作する。

## 13. 完了報告

実装完了時には、変更したファイル、追加・変更したルート、データ保存方法、実行したテスト、未対応事項があればその内容を簡潔に報告する。

---

## 14. 防災アプリホーム画面

既存のヘッダーのデザインと構造は変更せず、ホーム画面以外のページのデザインや機能も変更しない。既存のホーム画面ルート、気象情報API、テンプレート変数、認証処理を最初に確認し、既存方式を優先して必要最小限の変更で実装する。

### 表示順

画面上部から次の順番で表示する。各ブロックは白背景、薄い境界線、控えめな角丸と影を持つカードとし、ページ全体は明るく読みやすい背景にする。

1. 青森市 気象警報・注意報
2. 発信・指示ボードの住民向け情報欄
3. 避難所マップ
4. 避難所検索へのリンク
5. 災害報告・お問い合わせへのリンク

### 気象警報・注意報

- 見出しは「青森市 気象警報・注意報」とする。
- 対象地域を表示する。
- 見出し付近に「10分おき自動更新」を緑色で表示する。
- 警報・注意報がない場合は「現在、警報・注意報は発表されていません」を緑色で表示する。
- 警報・注意報がある場合は、警報名と状態を一覧表示する。
- データ取得時刻と気象庁発表時刻を表示する。
- 既存の気象情報APIを利用し、初回表示時に取得する。
- その後10分ごとに自動更新する。
- APIエラー時は画面を壊さず「気象情報の取得に失敗しました」と表示する。
- APIの空データ、欠落データ、通信エラーを安全に処理する。

### 住民向け発信情報

- 気象情報の直下に「発信・指示ボード」を表示する。
- 指示発信ボードのデータから、対象が住民向けの情報だけを表示する。
- 各情報に発信内容を表示する。
- 避難先が登録されている場合は「避難先: 施設名」と表示する。
- `@(高齢者)` などの強調記法は、ボード画面と同じルールで安全に強調表示する。
- 情報がない場合は「現在、住民向けの発信はありません。」と表示する。
- 未ログインユーザーも住民向け情報を閲覧できる。

データ例:

```json
[
  {
    "target": "住民",
    "content": "土砂災害の危険があるため避難してください",
    "shelter": "御所見小学校"
  }
]
```

### 避難所マップ

- 発信・指示ボード情報欄の直下に「避難所マップ」を表示する。
- 既存の地図機能、地図ライブラリ、地図データがある場合はそれを再利用する。
- 地図はカード内に収め、角丸で表示する。
- 固定高さまたは適切なアスペクト比を設定し、画面幅が変わってもレイアウトを崩さない。
- スマートフォンでは地図の高さを調整し、画面内に収める。
- 地図、通知、リンク同士が重ならないようにする。

### 避難所検索リンク

- 避難所マップの直下に「避難所を検索する」リンクまたはボタンを配置する。
- 既存の避難所検索ページへの正しいルートをURL生成機能で使用する。
- 必要に応じて矢印などの視認しやすい補助記号を付ける。

### 災害報告・お問い合わせリンク

画面下部に次の2つのリンクを配置する。

- 「災害を報告する」: 既存の災害報告先または緊急通報機能へリンクする。
- 「お問い合わせ」: 既存の問い合わせページ、フォーム、または設定済みの問い合わせ先へリンクする。

リンク先が未定義の場合、実在しない本番用連絡先を勝手に作らず、設定値として変更できるようにする。

### ホーム画面のログイン状態制御

- ヘッダーの見た目と構造は変更しない。
- 「避難所登録」と「発信・指示ボード」のメニュー項目はログイン済みユーザーだけに表示する。
- 未ログインユーザーには上記2項目を表示しない。
- 未ログインユーザーが保護URLを直接開いた場合も、既存の認証処理で保護する。
- 気象情報、住民向け発信、避難所マップ、避難所検索、災害報告、お問い合わせは必要な範囲で未ログインユーザーにも表示する。

### ホーム画面のデザイン要件

- 既存ヘッダーの見た目を維持する。
- 白または非常に薄いグレー系の背景を使用する。
- 見出しは青系、正常状態は緑系、警告・エラーは赤またはオレンジ系で表示する。
- カードの角丸は控えめにする。
- 情報の区切り、余白、文字サイズを整える。
- スマートフォンで横スクロールを発生させない。
- 既存CSS、テンプレート、コンポーネントの流儀を優先する。
- 不要なライブラリや大規模なリファクタリングを追加しない。
- JavaScriptは対象要素が存在するときだけ初期化する。

### ホーム画面の受け入れ条件

- ホーム画面の表示順が、気象情報、住民向け発信、避難所マップ、検索リンク、報告・お問い合わせリンクの順になっている。
- 気象情報が初回表示され、その後10分ごとに更新される。
- 警報なし、警報あり、APIエラーの表示を確認できる。
- 住民向け発信が気象情報の直下に表示される。
- 未ログイン時に避難所登録と発信・指示ボードがヘッダーに表示されない。
- ログイン時に避難所登録と発信・指示ボードがヘッダーに表示される。
- 避難所マップと避難所検索リンクが発信情報の下にある。
- 災害報告・お問い合わせリンクが画面下部にある。
- デスクトップとスマートフォン幅でレイアウトが崩れない。
- テンプレートエラー、構文エラー、主要ルートのHTTPエラーがない。
